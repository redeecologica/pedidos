<?php

// platform_check.php @generated by Composer
//
// Neutralizado intencionalmente (revamp PHP 8.4): o piso de PHP 5.5 declarado
// pelo PHPMailer fazia esta checagem lançar RuntimeException + HTTP 500 em TODA
// página enquanto a produção ainda estivesse em PHP 5.3 — ou seja, durante a
// janela entre enviar o código novo e virar o PHP para 8.4 no painel.
// O sistema roda em PHP >= 5.6 (referência local) e 8.4 (produção), ambos acima
// do piso; a checagem só traz risco. As classes do PHPMailer 6 são carregadas de
// forma preguiçosa (apenas no envio de e-mail), então não são tocadas no autoload.
return;
